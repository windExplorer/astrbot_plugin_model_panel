# 更新日志

## v1.4.4

卡片字体支持往 `data/fonts/` 丢文件；库改放插件专属数据目录，并把旧库自动搬过来。

### 需求（原话）

「参考一下我引用的项目，这里面生成的卡片用到了字体，咱们项目是不是也要加个字体，可以吗」
→「我就是 Docker 用户啊，应该咋整，不能用 astrbot 的特定目录吗，例如 data/fonts 啥的」。

### 先量了几件事（结论和直觉不一样）

- anima 的卡片字体**确实随包**，但不是仓库里那两个 25MB 的霞鹜文楷 ——
  `$includeList` 收了 `assets`（3.52MB 的 `ResourceHanRoundedCN-Medium.woff2`）而**没收 `fonts/`**，
  文楷只是开发素材。所以 anima 用户看到的也是圆体。
- **Pillow/FreeType 能直接加载 woff2**，不用先转 ttf（实测 `getname()` 正常返回）。
- 同一张卡换字体，渲出来高度 678 / 698 / 714 都不一样 —— 版式跟着字体走，
  这也是「不打包字体」时跨机器不一致的代价。
- 打包字体的性价比：一份全量中文 3~25MB，本插件 zip 才 1.07MB。**不打**。
  改成「投放目录自动扫」，Docker 用户丢个文件就完事，连重装都不用。

### 字体（`card_render.py`）

- 查找顺序改为 **`card_font_path` 指定 → `data/plugin_data/<插件名>/fonts/` →
  `data/fonts/` → 系统字体**。目录不存在就是空候选，不抛错；
- 认 `.ttc/.otf/.ttf/.woff2/.woff`，一个目录里躺着好几个时按
  **霞鹜文楷 > 圆体 > Noto > 思源 > 苹方 > 雅黑** 排，不认识的名字垫底而不是挡路；
- **点保存配置就重新探测**（`api_set_config` 里 `resolve_font(force=True)`）：
  用户很可能就是刚丢了字体才来点保存，不重探就得再重启一次 AstrBot 才见得到效果。

### 数据目录（`main.py`）——顺手修掉一个真 bug

- 原来 `initialize()` 里那句 `getattr(self.context, "data_dir")` 是**死分支**：
  4.28.1 的 `Context` / `Star` 根本没有 `data_dir` 属性（全仓搜 `self.data_dir` 只有无关类命中），
  于是永远落到 `DEFAULT_DB_PATH = os.path.join("data", "model_panel.db")` ——
  **相对 CWD**。容器里 `WorkingDirectory` 不是 AstrBot 根目录时，库会写到别处，甚至攒出两份。
- 现在走 `StarTools.get_data_dir(PLUGIN_NAME)` → `data/plugin_data/astrbot_plugin_model_panel/`
  （核心助手不可用时手工拼同一路径）。**不是插件安装目录**：`data/plugins/<名>/` 每次更新会被
  整个 `remove_dir`（`core/star/updater.py`），库放那儿等于每次升级清零；`plugin_data` 只在卸载时删。
- 启动时自动把旧库连 `-wal` / `-shm` 搬过去（WAL 里可能还有没 checkpoint 的事务），
  **目标已存在则一律不动**。搬不动只 warning，不打断启动。

### 自检

新增 `tests/test_font_and_db_paths.py`（22 项）：目录排序与扩展名过滤、查找顺序、
插件目录名与 `main.PLUGIN_NAME` 一致、`StarTools` 不可用时的回退、旧库搬运含 WAL 边车、
目标已存在不覆盖、路径去重不含自身、搬不动时返回 None 且留日志。

**它当场抓到一个会造成数据丢失的 bug**：`_migrate_legacy_db` 最初漏了「目标已存在就收手」的守卫，
新库会被老路径上残留的文件直接盖掉。修完做反向对照（删掉那两行守卫）→ 立刻 2 项 MISS。

另：本仓库自带的 `test_card_layout` / `test_card_rows` / `test_merge_ledger` 等一并复跑，全绿。

## v1.4.3

新增 `/模型帮助`；`/切换系统模型` 的结果改用卡片；README 整体重写。

### 需求（原话）

「1. 切换系统模型之后的结果应该用卡片展示。2. 加个帮助指令：模型帮助（也是只能管理员），
也是发送卡片给用户 3. readme文档给更新一下，写好一点，简洁明了。」

### 改动

**切换结果卡**（`_apply_default_model`）

- 切换成功不再回一句纯文本，而是发一张**结果卡**：大字写新模型名，
  行块两行「切换前 → 默认」（新模型高亮 + `默认` 胶囊），脚注提示
  「下一条对话就会走新模型」「发 /切换系统模型 可再换」；宽 760（只有两行，用不了 1080）；
- 「切换前」也按 provider 表翻译成展示名，不再是裸 provider id；
- 字体不可用时照旧降级成原来的文字回执。

**`/模型帮助`**（`cmd_model_help`，管理员限定）

- 一张速查卡：8 条指令各占一行，行注写清「花不花额度、回序号能干什么」；
  脚注带管理员 ID 的配置位置与「旧名 /检测模型 仍可用」；
- 新增指令登记进 `tests/test_static_guards.py` 的 `EXPECTED_COMMANDS`
  （那条「清单覆盖所有 cmd_* 方法」的守卫会逼着新指令必须登记，清单不会腐烂）。

**README 重写**

- 从 397 行压到 ~160 行，按「是什么 → 安装 → 指令 → 卡片 → WebUI → 对外接口」重排；
- 大量 v1.x 演进过程的口水利落掉，只留**当下有效的口径**（两本账、埋点优先、
  8 位小数、直发不 @、管理员 ID 前置、全局锁）；细节归 CHANGELOG。

### 自检

静态守卫、选单流程、版面像素、WebUI 静态全绿；两张新卡（切换结果 / 帮助）出图人工过目。

## v1.4.2

单价输入框的小数位从 4 位放宽到 **8 位**。

### 现象

用户：「费用的填写小数点后面多搞几位，现在好像只有 4 位，至少搞 8 位吧？」

### 根因（只在前端）

`n-input-number` 的 `precision` 会把值**静默四舍五入**：单价输入框以前写 `precision: 4`，
填 `0.00025` 这类便宜模型的缓存价 / 按量价，会被悄悄改成 `0.0003` ——
界面上没有任何提示，保存也「成功」，但花费已经算错了。
后端是 REAL 存原值、从不舍入（金额只在**算完之后** round 到 6 位），所以限制只在前端。

### 改动

- 模型档案的四个单价（输入 / 输出 / 缓存命中 / 按次）`precision` 统一放宽到 **8**，
  步进也调小到对应量级（0.01 / 0.01 / 0.001 / 0.0001）；
- 模型级**倍率覆盖**从 2 位放宽到 4 位（1.005 这类中转加价以前会被舍成 1.0）；
  分组级的倍率输入框本来就没设 precision（不限制），不动；
- 档案抽屉的填价提示注明「小数最多可填 8 位」。

### 自检

`tests/test_webui_static.py` 新增静态守卫：`PRICE_FIELDS` 里的每个 `precision` 必须 ≥ 8，
否则报错 —— 这类回归在界面上毫无提示（数字被改了但保存「成功」），只能静态拦。

## v1.4.1

模型管理页分组的「今日次数」改由逐次埋点出，并补上成功 / 失败拆分。

### 现象

用户：「模型管理的分组的今日次数显示的有问题，对不上，而且成功次数和失败次数也没有展示呢。」

### 根因

分组「今日次数」取的是 `llm_usage`（`on_llm_response` 钩子写的）：

1. 那个钩子**跳过流式分片**、且只在响应带 usage 时才写 —— 现在大多数 provider 都是流式，
   调 10 次可能只记两三次，于是和实时监测对不上；
2. 它只记「带用量的成功响应」，**没有成败概念** —— 想展示成功 / 失败也没有数据。

### 修法

- 新增 `storage.calls_day_by_provider()`：按天按 provider 聚合 `llm_calls`
  （总次数 / 成功 / 失败 / 被取消），口径与实时监测、与首页调用统计完全一致；
- `_cost_rollup` 的 `calls_today` 改用它，并新增 `calls_ok` / `calls_fail` / `calls_aborted`
  三个字段（分组与单模型都带）；`today` / `week` 花费仍按 llm_usage 的 token 算（口径不变）；
- 模型管理页分组头部在限额 chip 旁新增「✓ 成功 N · ✗ 失败 N」，悬停有口径说明；
- 首页 Token 用量卡的「今日请求次数」同步改用 llm_calls 的今日总数
  （否则同一个页面出现两个对不上的请求次数）。

### 口径说明（也写进了悬停提示）

`llm_calls` 含 AstrBot 后台的「提供商测试」、WebChat 与其它插件直调 provider，
**不含检测**（探测走自己的表、埋点对它抑制）。如果拿供应商后台的配额计数来对账，
记得对方通常把检测也算进去 —— 那是口径差异，不是漏记。

### 自检

`tests/test_storage_scopes.py` 新增：按天按 provider 的聚合（总 / 成功 / 失败 / 取消）、
`calls_totals` 的累计与今日口径、按别的日子查询过滤生效。

## v1.4.0

调用次数统计 + 实时监测覆盖 AstrBot 侧调用。（按新版本号规则，含新功能升中间位。）

### 需求（原话）

1. 「次数统计应该要算上所有调用的次数，和实时监测里面用的应该是 一样的，
   可以细化一下，成功次数和失败次数。」
2. 「实时监测的最后一次调用…应该不仅仅是窗口内的调用，还有 astrbot 里的调用，
   比如我在 astrbot 的提供商里面测试模型也算是，还有 astrbot 的 chat 界面。」

### 根因

调用台账（`_merge_ledger`）只读两路：核心 `provider_stats`（agent 对话轮）与探测历史。
而 AstrBot 后台的「提供商测试」、WebChat 聊天界面、其它插件直调 provider 的调用
**不写 provider_stats**，只存在于本插件埋的 `llm_calls` 里 —— 于是它们既不进
「最后一次调用」，也不进任何次数统计。首页的「次数统计」更窄：它数的是**检测**
（`test_sessions`），与「调用了多少次模型」是两件事。

### 改动

- **台账三路合并，埋点优先**：`_merge_ledger` 接受第三路 `call_rows`（llm_calls 原始行）。
  规则：有埋点数据时，**次数与「最后一次调用」全部改由它出**（口径 = 每次尝试），
  `provider_stats` 只贡献它独有的 **token**；埋点没有数据时回落核心表（老行为）。
  **两路绝不能相加** —— agent 一轮对话两边各记一条，加起来就是双倍计数。
- 新增 `storage.calls_window()`（原始行给台账算 P95 / 最近一次）与
  `storage.calls_totals()`（累计 + 今日的总数 / 成功 / 失败 / 中断）。
- 首页「历史检测」卡新增**调用次数统计**：总次数 / 成功 / 失败（累计）+ 今日一组，
  来源与实时监测相同（`calls_totals`），并注明「含 AstrBot 后台的提供商测试与 WebChat」；
  下面的检测统计（会话数 / 检测条数）保留，两件事不再混在一个数里。
- 四个语言的文案都已补齐（zh/en/ja/ko）。

### 自检

新增 `tests/test_merge_ledger.py`：无埋点时回落核心表、有埋点时次数与最近一次由它决定
（且**不与核心表相加**）、token 仍来自核心表、AstrBot 侧只进 llm_calls 的调用可见、
探测与埋点取更新的那个、延迟 P95 来自埋点原始行。

### 环境备注

本机 uv 的受管 Python 目录被 Windows 安全策略标记为「不受信任的装入点」（os error 448），
自检改用独立 venv 跑（`%TEMP%\mp_venv`）。与插件本身无关。

## v1.3.21

新指令「/全部模型检测」+ 卡片主题重整 + 固定语义色（报错红 / 告警橙 / 恢复绿）。

### 1. /全部模型检测

用户原话：「model_panel 怎么用指令一次性检测全部模型，要不加个指令：全部模型检测；
检测中也要发消息提示，避免任务没跑完就加新的检测任务了；检测完之后发送最新的模型状态卡片。」

- **不挑通道**：加载中的对话模型全部测一遍。`/模型检测` 与它的分工是
  「我配了哪些检测通道」与「我现在想把所有模型都打一遍」是两个不同的意图；
- **三重防重入**：共用那把全局锁（撞车直接明说，不排队）→ **先占锁再回执** →
  开跑前发「检测中」卡（带并发数与最坏耗时）；
- **跑完发最新的模型状态卡**：行底是状态机**现在**的判定，行内胶囊是**这一轮**的结论
  ——「默认 / 静音」（身份）与「本次通过（绿描边）/ 本次失败（**实心红**）」（结果）
  经常同时成立，挤掉一个就等于让用户二选一地丢信息，所以胶囊支持画多颗；
- 结果同样喂进状态机与告警链路（v1.3.15 那套共用实现）。

配套改动：`render_card` 支持行内多颗胶囊（`_pill` 返回右边界、可实心）；
`rows=[]` 时不再留一条空白带（提示卡只有头 + 脚）；
`_overview_chain(event=None, …)` 供后台任务发卡。

### 2. 卡片主题重整（两插件同步）

用户原话：「默认的主题颜色靛蓝，为啥我看着是紫色，你改一下吧，紫色的也不要删了，可以再加点主题。」

- `indigo` **重配色成真的蓝**（强调色 `(37,99,235)`）：旧版那套（`(79,70,229)`）确实偏紫；
- 旧配色**没删**，改名 **`violet`（紫罗兰）**——它已经出现在很多人的配置里，
  直接改掉会让他们的卡片悄悄变色；
- 新增 `crimson`（朱红）、`amber`（琥珀橙），两边对齐为同一套 8 个：
  `rose` / `indigo` / `violet` / `teal` / `amber` / `sunset` / `crimson` / `graphite`；
- `THEME_CHOICES`（控制台下拉）与 `card_theme` 的 `labels` 同步更新。

### 3. 固定语义色：报错红 / 告警橙 / 恢复绿

用户原话：「能不能将一些提示卡片固定颜色主题，例如报错用红色，告警用橙色…
不过我没分清楚什么时候是报错消息卡片，什么时候是告警消息卡片。」

- 新增 `TONE_THEMES`：`error → crimson`、`alert → amber`、`ok → teal`，
  `_card_png(tone=…)` 会**强制**换主题，与 `card_theme` 偏好无关；
- 三张告警卡（故障 / 恢复 / 限时免费到期）分别固定为 橙 / 绿 / 橙，
  角标（告警 / 恢复 / 提醒）与颜色说的是同一件事；
- 分不清的问题用一张表写进 README：**橙 = 有事但知道是什么事，等一会儿；
  红 = 出了没预料到的错，先看日志**。

## v1.3.20

`external_available()` 多报一个 `busy`（此刻有没有检测在跑），`api` 2 → 3。

### 为什么

联动方（权限控制台的 `/切换模型检测`）点下去之前就能知道「现在有没有在跑」，
于是**不必先发一张「检测中」卡片、再补一句「其实没跑」** —— 那两句是自相矛盾的，
用户明确反馈过。

以前只有 `external_detect()` 的 `busy=True` 返回值能表达这件事，
而那是**调用之后**才知道的；现在探活接口里就带上了锁状态。

注意这只是一个**瞬时快照**：真正开跑时仍可能撞上（同一秒里两个人点），
所以 `external_detect()` 的 `busy` 返回值必须保留 —— 两边各管一段。
读这个字段不做任何副作用，也不碰数据库。

## v1.3.19

配合 user_gateway 的「整组一起测」：回序号多选上限 10 → 30，估时改成按并发算。

### 背景

用户反馈（针对联动方，但本插件自己那条 `/模型检测` 有同样的问题）：
「这个配置项限制了 1-30 个不太好…应该直接检测该分组所有可选模型一起测啊。」

### 改动

- `_PROBE_MAX_PICK` 10 → **30**：回一个**分组序号**却被告知「太多、少选几个」，
  与「整组一起测」这个用法是矛盾的。真管成本的是每日预算与并发上限（monitor 的护栏），
  不是这个截断；
- 指令回执里的估时改成**按并发轮数**算：`ceil(N / 并发) × 超时`。
  早先写成 `超时 × 个数`，10 个模型会报「最坏约 450s」—— 实际 3 个并发只要 150s，
  这种数字只会把人吓退；
- `external_available()` 从静态方法改成实例方法，并多报两个字段：
  `probe_concurrency` / `probe_timeout`（联动方要用它们估时，而并发数是本插件定的，
  它猜不到也不该猜）；`api` 1 → 2；
- 并发数收敛到一个出处 `probe_parallelism()`（`_probe_batch` 原本自己算了一遍）。

## v1.3.18

修 `get_model_states()`：状态机一旦写出错误码就再也读不出来（`/切换模型检测` 报
`invalid literal for int() with base 10: 'unknown'`）。

### 现象

权限控制台发 `/切换模型检测`，回一句：

```
检测失败：invalid literal for int() with base 10: 'unknown'
```

### 根因（`storage.get_model_states`）

```python
for c in cols:
    d[c] = r[c] if c == "state" else int(r[c] or 0)   # ← cols 里有 last_error_code
```

`model_state` 的七列里，`state` 与 **`last_error_code` 都是 TEXT**（后者存的是
`timeout` / `auth` / `unknown` 这种码），其余才是整数。这一行却只把 `state` 排除了，
于是只要**任何一行**的 `last_error_code` 非空，`int('unknown')` 就抛。

**而 `save_model_state` 是先读后写的** —— 于是第一次写入错误码之后，
状态机就再也写不进任何东西：面板永远停在最后一次成功的结论上。
这正是 v1.3.15 想解释的那个「刚测出故障、面板还是绿的」的**真正元凶**
（当时我只补了「外部检测也要喂状态机」，没发现喂进去的写入本身会炸）。

为什么一直没人看见：定时巡检这条路把异常吞在自己的 `try` 里，只在日志留一行
warning；而这次 `/切换模型检测` 是**同步调用**，异常被原样顶到了用户面前。

### 修法

- 按列区分类型：`state` / `last_error_code` 读成字符串（`None` → `''`），
  其余走新的 `_as_int()`（转不了给默认值 —— 面板是观测工具，一列坏值不该让它整个打不开）；
- 写入侧不动：`save_model_state` 的读-改-写语义本来就是对的。

### 自检

`tests/test_storage_scopes.py` 新增「状态机往返」：写入 down + `last_error_code='unknown'`
再读回（错误码原样、数字列仍是 int），并**再写一次**（旧版连这一步都到不了）。

已验证这条自检能抓到旧代码：把修复 stash 掉后它立刻报
`ValueError: invalid literal for int() with base 10: 'unknown'`。

## v1.3.17

卡片加宽 + 状态改用「行底横向渐变」（去掉行首小圆点）。

### 需求（原话）

「1. 加宽一点（模型名字长了有点看不太全）2. 状态不要用点展示，可以用浅色从左往右的渐变背景。」

### 改动

**加宽**

- `CARD_W` 940 → **1080**，`CARD_W_MAX` 1160 → **1340**；
- 明细卡（六个数值列）显式给到 **1160**，告警卡 1040；
- 顺带：去掉行首那颗点之后，名称列左侧又收回 20px。

**状态靠行底渐变表达**（`_hgradient` / `_row_bg` / `_row_left` / `STATE_TINT`）

- 每行行底 = **左端掺状态色 → 右端回到中性底**的横向渐变，四角仍然圆；
- 浓度按状态分档：正常 `0.90`（只掺 10%，几乎看不出）、降级 `0.66`、故障 `0.60`、
  无数据 `0.88`。**反常的那几行才该跳出来**，整屏都是彩条等于没有重点；
- 左深右浅：右边是模型名与数字，底色越接近中性越清楚；
- 分组行也走同一套（左端 13% 强调色 → 右端 4%），与状态渐变行视觉上是一家的；
- 删掉模型行的 `_state_dot` 调用；`_state_dot` 保留给**头部那行图例**（「● 正常 9」）——
  它是图例，小点更合适；
- 渐变只铺 96 个色阶再 BILINEAR 放大（逐列画竖线一行要 1080 次 draw，纯属浪费）。

### 自检

`tests/test_card_layout.py` 新增像素断言（rose / graphite 两套主题各一遍）：

- 正常那行底色**偏绿**、降级**偏琥珀**、故障**偏红**（直接取像素比通道）；
- 故障行**左深右浅**（左端 `(236,171,183)` → 右端 `(253,245,250)`）；
- 原来的墨迹阈值从「写死 600」改成**按该行自己的背景算比例**：v1.3.17 的故障行左半边
  亮度已经贴近 600（实测 590），阈值再浓一档就会把整行背景当成文字 —— 那时它仍会「通过」，
  只是量的东西已经不是文字了。

## v1.3.16

指令回执改成直发：卡片不再 @ 发送者。

### 现象

用户反馈：发 `/模型状态`（以及其它带卡片的指令）时，卡片前面总跟着一个「@ 发送者」。
私聊里多余，群里更明显 —— 每来一张卡片就 @ 一次。

### 根因（不在本插件里）

AstrBot 的 `ResultDecorateStage` 会给结果链做装饰，其中一段是：

```python
can_decorate = all(isinstance(item, (Plain, Image)) for item in result.chain)
if can_decorate:
    if self.reply_with_mention and event.get_message_type() != MessageType.FRIEND_MESSAGE:
        result.chain.insert(0, At(qq=event.get_sender_id(), name=...))
```

开关是全局的 `platform_settings.reply_with_mention`。我们的卡片**恰好是一张 `Image`**，
于是命中 `can_decorate`，被插了一个 `At`。
（同一个 stage 还会做「长文本转图 / 超长转合并转发」，但那些只影响纯文本。）

### 修法

新增 `_reply()` / `_reply_chain()` 两个直发封装（`event.send(MessageChain(...))`），
全部指令回执改走它们：

- `event.send()` **不进结果链**，结果链上的装饰就都绕过去了 —— 与 user_gateway 的做法一致；
- 覆盖范围：`/模型状态` `/模型统计` `/模型检测` `/切换系统模型` `/模型静音` `/陪伴分数`
  以及回序号那条路（`on_number_pick`）的全部回执（图片卡片 + 纯文本提示）；
- 顺带的好处：直发会置位 `_has_send_oper`，本轮不会再触发一次 LLM 回复；
- 代价：装饰链里的「长文本转图 / 超长转转发」也不再生效。对卡片无所谓（本来就是图），
  对文本回执反而更可控（原样送达，不会被改写）；
- 告警推送（`context.send_message` 给管理员 / 群）本来就是直发，不受影响。

### 自检

- `tests/test_static_guards.py`：
  - 原来断言「cmd_* 必须是 async generator」——那是 `yield` 时代的代理指标，
    现在改为「是 async 函数」（AstrBot 的 `call_handler` 两种都支持）；
  - **新增**静态拦截：源码里出现 `event.plain_result(...)` / `event.chain_result(...)` 即失败。
    这类回归编译过、单测过，只有群里看得出来，必须静态拦住。

## v1.3.15

外部检测的结果也要喂进状态机，否则「刚测出故障、面板还是绿的」。

### 问题

v1.3.14 加了 `external_detect()`（给权限控制台的 `/切换模型检测` 用），但当时它**只写了历史**，
没有更新 `model_state`、也没走告警链路。后果是：联动方刚打完一轮，某个模型明明挂了，
用户回头打开面板却还是「正常」—— 面板显示的仍是上一次的结论。
而用户对面板的信任就建立在「我刚测过，这里就是最新的」上。

### 修复

- 把定时巡检里那段「探测结果 → 状态机 + 告警」抽成 `_apply_probe_results(results, names, now, cfg)`，
  **手动 / 定时 / 外部三条路共用**：`evaluate_monitor` 的状态判定、错误码与失败原因的补写、
  `save_model_state` 落库、`_dispatch_alerts` 投递（含冷却）只此一份。
- `external_detect()` 探测完先调它再读快照，所以返回给联动方的 `items` 已经是**更新后**的状态。
- 定时巡检的告警名字沿用「供应商/模型」全名（与面板一致），不是裸模型名。

### 为什么这件事值得单独发一版

口径分叉（手动更新状态、定时更新状态、外部不更新）是那种**只在一个入口下才出现**的 bug：
自己用 `/模型检测` 测是好的，只有从联动方那边测才不对，而两边的现象都只是「面板数字没变」。
唯一稳妥的做法就是所有探测入口共用同一段实现。

## v1.3.14

对外开放「检测」与「快照」两个接口（供权限控制台的「切换模型检测」联动），卡片新增「更新」列标注数据时间。

### 需求

权限控制台（`astrbot_plugin_user_gateway`）要加一条「切换模型检测」：检测逻辑**走本插件**、
结果也**存在本插件**，那边只负责发卡片、管权限与冷却。所以本插件得有两个稳定的对外方法；
另外两边都要能看出「这行延迟/成功率是什么时候的数据」。

### 修复 / 新增（后端）

- **抽出 `_probe_batch(targets, timeout)`**：并发探测一批模型，**只跑探测**（不写历史、不发消息、
  不占锁），指令检测 / 定时巡检 / 外部检测三条路共用一份「怎么算探测成功」的实现。
  同名逻辑抄三份的话，迟早出现「同一个模型在你那边绿、在我这边红」。
  另抽出 `_record_probe_history(trigger, results)` 统一写历史。
- **新增 `external_snapshot(provider_ids, days)`**：给别的插件读的只读快照 ——
  不请求模型、不花钱。每个模型返回 `state / available / latency_ms / avg_latency_ms /
  success_rate / samples / last_ts / last_ok / last_error_code / last_probe_ts`。
  两个时间戳的区别写进了 docstring：`last_ts` 是**最近一次有数据**（真实对话或探测都会更新它），
  `last_probe_ts` 才是最近一次**探测** —— 联动方的「模型级冷却」按后者算才准。
- **新增 `external_detect(provider_ids, timeout)`**：真打一次模型（**会花钱**），结果写进本插件历史，
  返回最新快照。与手动/指令/定时探测**共用同一把全局锁**，撞车时返回 `busy=True` 而不是排队
  —— 排队只会让两边一起超时。触发来源记为 `gateway`，面板上显示「插件联动」，
  这样「谁花的这笔钱」一查就知道（`_probe_source` + WebUI 的 `SOURCE_LABELS`）。
- 新增 `external_available()`：给联动方探活用的静态信息（不碰库、不碰网络）。

### 修复 / 新增（卡片）

- 概览/明细卡新增 **「更新」列**：相对时间（`刚刚` / `3 分钟前` / `2 小时前` / `3 天前`）。
  延迟与成功率都是**历史统计值**，光给数字看不出新鲜度。
  时间戳**跟着延迟数字的来源走**（有逐次埋点用它那一条的 `ts`，否则用合并台账的），
  避免「延迟取自 A、时间取自 B」这种对不上的组合；一条记录都没有时给 `-`，
  而不是「刚刚」那种「看着很新其实没数据」的假象。超过一个月退回绝对日期。
- 卡片宽度 880 → 940、名称列下限 230 → 300：多了一列之后模型名被挤成
  `gpt-4o-mini-2024-07-…` 了，宁可整张卡宽一点也别让名字看不出是哪个。

### 自检

`tests/test_card_rows.py` 补「更新于」三组断言：逐次埋点路径取它自己的 ts、
没有埋点时退回台账的 last.ts、以及 `_fmt_ago` 的档位（刚刚 / 分钟 / 小时 / 天 / 无时间给 `-`）。
（写这组断言时踩了一次自己的坑：最初用写死的「未来」时间戳当基准，结果全部算成「刚刚」——
测试成了永远通过的摆设，改成取真实时钟。）

## v1.3.13

卡片不再自称「实时」：标题改回「模型状态」、角标改「只读」，并把数据来源写进脚注。

### 问题

用户原话：「右上角的实时有点搞笑，本来就不是实时的，只是最近一次调用产生的记录。」

说得对：这张卡读的是**已经发生过的调用**（逐次埋点 + 探测历史），一次模型请求都不会发。
标成「实时」会让人以为数字是刚打出来的，于是「模型明明挂了、卡上还是绿的」会变成一场
没必要的排查 —— 而实际原因只是「挂了之后还没人跟它说过话」。

### 修复

- `/模型状态` 卡片：标题 `模型实时状态` → `模型状态`（与指令同名），
  右上角角标 `实时` → `只读`（说明这张卡的性质：不请求模型、不花钱）。
- 脚注新增第一句 `数据来自真实调用的记录，不是此刻探测`，
  并保留口径句「延迟为最近一次调用耗时；成功率为今天窗口内统计」。
- `card_render` 的模块文档与 `badge` 参数说明里补一句「别用它写『实时』」，
  免得下次又顺手写回去 —— 这类标签一旦写错，看卡片的人不会知道数据其实是旧的。
- README：卡片示意改画 `只读`，并新增一段「这张卡不是实时的」，
  明确「刚挂但还没被调用过」的模型仍显示上一次结论、想立刻确认就发 `/模型检测`。

## v1.3.12

修掉「延迟 / 成功率」表头与数值差一个字宽的对齐 bug，卡片配色可换（五套主题）。

### 问题

用户原话：「之前那个状态卡片，延迟和成功率列和数据没有对齐啊，并且看起来有点丑，
可配置颜色主题吗」。三条里前两条都是同一个错引起的，第三条是新需求。

### 根因（第 1 条是纯粹的排版计算错）

`render_card` 画行内数值时**同时**用了两种右对齐：
`draw.text((right - _text_w(...), cy), txt, anchor="rm")` ——
`anchor="rm"` 已经把**步进宽度**的右端对齐到给定的 x 上了，再手动减一次文本宽度，
等于右移量被减了两遍，整列**固定左移一个字宽**。

实测（把渲染结果按像素扫墨迹）：表头「延迟 / 成功率」在 x=847，下面每一行的数值都在 x=778，
偏差 69px。而且因为列宽仍是按「贴右」算的，名字的可用宽度也跟着算错，
于是长模型名会紧贴着数值列 —— 截图里 `gpt-4o-mini-2024-07-18` 后面直接接 `1分45秒` 就是这么来的。
这就是「看起来有点丑」的来源：右侧一大片空白 + 左边挤在一起。

改对之后又发现**第二层**偏差：`anchor="rm"` 对齐的是步进宽度而不是墨迹。
同一个锚点下，末位是汉字（`1分45秒`）还是拉丁字母（`812ms`），右侧留白差 1~2px；
表头（17px）与数值（24px）字号不同还会再放大。于是新增 `_draw_right()` 按**墨迹右边缘**对齐
（用 `font.getbbox()` 的右偏移做修正），表头与数值共用同一个函数，不再各写一套。

### 修复（后端）

- 数值与表头统一走 `_draw_right(draw, right, cy, text, font, fill)`：按墨迹右边缘对齐，
  并顺带把「按墨迹右对齐」的意图写进函数名与文档 —— 这类错编译、运行都不报，
  只有人眼看图才发现，必须把正确写法固定成一个函数。
- 列宽计算（`_column_widths`）改用**墨迹宽**：列宽决定两列之间看得见的空隙，
  按步进宽算会让两列比设定值更挤。
- **新增 `card_theme` 配置**，五套主题：`rose`（樱粉，默认）/ `indigo` 靛蓝 / `teal` 青碧 /
  `sunset` 落日橙 / `graphite` 石墨灰。主题表在 `card_render.THEMES`，
  候选名单是 `card_render.THEME_CHOICES`（**唯一来源**，通过 `/panel/config` 下发给前端，
  前端不抄第二份，否则以后加主题会「下拉里没有、后台也选不到」）。
  不认识的主题名回落到默认，**不会让卡片画不出来**；改完下一条卡片即生效，不用重启。
- 顺手把版面调顺眼：
  - 序号方块改三态 —— 实心（**只给「当前默认」那一行**）/ 淡色底（普通模型行）/ 描边（分组行）。
    满屏都是实心亮块等于没有重点，现在一眼就能找到默认模型那一行。
  - 行底改成跟着主题走的淡色调（不再是中性蓝灰）：粉色的卡片配蓝灰块是「看着别扭」的另一半原因。
  - 列头文字加深（原来那档太淡，像没渲染完）。

### 修复（前端）

- 设置页新增「卡片配色主题」（下拉）与「卡片中文字体路径」（输入框）；
  主题下拉的选项**由接口下发**，不写死在前端。设置页标题改为「检测与卡片配置」。
- 新文案直接写中文（本仓库约定不再新增多语言文案），既有语言文件未改动。

### 自检

新增 `tests/test_card_layout.py`：**直接把渲染出来的 PNG 扫一遍像素**，
断言「列头带的右墨迹边缘 == 每一行数值带的右墨迹边缘（偏差 ≤ 2px）」且数值列没有顶到卡片边沿。
这类 bug 是「编译通过、运行不报错、功能全对，只是图难看」，只能靠量像素来钉住。
测试在没装 Pillow 或没有中文字体时打印 skip 并返回 0，不会拖垮其它自检。

## v1.3.11

故障告警卡片补上「失败原因」：错误码单列一列、原因写在行里、脚注汇总整批的错误分布。

### 问题

收到的告警只有一句「连续失败 3 次」，没有一句「**为什么坏**」。
而这条消息的唯一用途就是让人决定「要不要现在处理」——是中转站 502、key 被限流，
还是模型名写错了，处置方式完全不同。用户原话：「看了下告警卡片，只显示了失败，
能不能附带一下失败原因啥的」。

### 根因

- `monitor.evaluate` 判定故障时只写 `reason = "连续失败 N 次"`，而错误码取的是
  `prev["last_error_code"]` —— **上一轮**状态里存下来的那个值。首次告警时上一轮还没有码，
  于是这一格必然是空的。
- 错误**原文**（`error_message`）压根没从记录里读出来。逐次表 `llm_calls` 明明同时有
  `error_code` 与 `error_message` 两列（v1.3.9 建的），判定时被丢掉了，等于自己埋的点白埋。
- 另一个数据源天生没有原因：核心 `provider_stats` 只记 `status`，不记错误码与原文
  （见 `stats_reader.summarize` 的注释）。所以这条路下**必须如实写「原因未知」**，
  不能编一个「未知异常」糊过去 —— 那会让人以为查过了。

### 修复

- `monitor.evaluate` 在遍历本批记录时顺手收集三样东西并放进 `Decision.detail`：
  `error_codes`（各错误码出现次数）、`last_error_code` / `last_error_message`
  （最近一次失败的码与原文，原文经 `redact` 脱敏并截到 90 字）。
  `Decision.last_error_code` 也改成优先取**本批**的码，本批没有再回落到上一轮状态。
- 定时探测路径 `_probe_round`：那批 `CallRecord` 是硬凑出来的（没有 error 字段），
  于是把探测结果里的 `error_code` / `error` 补进 decision 的 detail。
- `_alert_chain`：故障告警的列从「连续失败 / 成功·样本」改成
  「连续失败 / 成功·样本 / **错误码**」；行副标题写成
  `连续失败 5 次｜超时×5 · Request timed out after 45s (gpt-4o-mini)`；
  脚注新增一句 `错误分布：超时×5 · 鉴权失败×3`（一家供应商挂十个模型时，
  一句话说清这波是什么在坏比十行重复有用）。
  拿不到原因时按实际情况给不同的下一步：全都拿不到 → 「核心统计表不记失败原因；
  发 /模型检测 可复测拿到具体报错」；只有个别拿不到 → 「N 个模型拿不到原因（核心表不记）」。
- `open_alert` 存进告警事件的 `detail` 也带上原因：**补发**那条纯文本消息直接读它，
  否则补发出去的还是同一句没说清的话。
- `/模型检测` 的结果卡片：失败行不再只印错误码，一并给出原始报错（同样脱敏截断）。
- 卡片宽度 960 → 1000：多了一列「错误码」，名字还得放得下。

### 自检

`tests/test_monitor_alerts.py` 补三组断言：
① 原因跟着判定一起出来（错误码分布 / 最近一次的码 / 原文）；
② 核心表拿不到原因时字段留空、不编造；
③ 错误原文里的 `api_key=sk-…` 必须被脱敏（告警是发到聊天软件里的）。

## v1.3.10

四个聊天指令统一成同一张卡片（与「萌萌权限控制台」的「切换模型」同款版面），分组也能回号、检测支持多选；`/模型静音` 改成全局总开关。

### 问题

1. **卡片看不清**：序号被写进名字里（`3. gpt-4o-mini-2024-07-18`），模型名一长，序号先被截掉——
   而这张卡的用法恰恰是「照着一个号回数字」。用户原话：「卡片风格做成和权限控制台那张类似，序号都要加上」。
2. **`/模型统计` 只能靠关键词**：模型名（`NVIDIA/deepseek-ai/deepseek-v4-flash-0731`）根本记不住，
   记不住就查不了；`/模型检测` 一次只能回一个序号，想批量测要来回发好几条指令。
3. **分组没有序号**：组头只有组名，想「把这个供应商下的模型都测一遍」只能逐个点。
4. **`/模型静音` 的粒度不对**：它是逐模型的（还要记 `6h` / `2h` 这种时长），
   而实际场景是被多家供应商的告警一起刷屏 —— 这时真正想按的是总闸。
5. **排序不稳定**：按「组内最坏状态」排序，同一份数据刷新两次顺序可能不同，
   而序号是要照着回的（顺序一变，用户回的数字就指到别的模型上）。
6. **`/切换系统模型` 回数字毫无反应**（v1.3.9 起的 bug）：`on_number_pick` 里写的是
   `await self._apply_default_model(...)`，而后者是 **async generator**，
   `await` 它直接抛 `TypeError: object async_generator can't be used in 'await' expression`
   —— 回执一条都发不出去，用户在群里看到的就是「bot 装死」。
7. **`cmd_model_probe` 被定义了两次**（同一套代码上下各一份，前者是死代码，
   压在上面的那份永远不执行）。`tests/test_static_guards.py` 取的是 `methods` 字典里的最后一个，
   所以这个重复一直没被测出来。

### 根因

第 6 条是纯粹的「await 与 async generator 混用」：带 `yield` 的函数调用后拿到的是异步生成器，
必须 `async for` 消费，`await` 会直接抛错。之所以一直没暴露，是因为
`/切换系统模型 kimi` 那条带关键词的路径走的是同一个错，而报错被吞在生成器里没人看日志。

第 1~5 条是同一个设计问题：卡片是**顺手拼出来的**（每张卡各自决定怎么写），
而不是「一份序号 → 一张卡」。序号在文字里、分组没有号、排序跟着状态走，
都是「没有把序号当成一等公民」的后果。

### 修复（后端）

- **重写 `card_render.render_card`**（新版面沿用 `astrbot_plugin_user_gateway/model_card.py` 的骨架）：
  渐变头（大字结论 + 状态色汇总点 + 右上角小标签）、白底行块（**实心序号方块** + 状态色点 + 名称 +
  行内胶囊 + 右对齐数值列）、浅色脚（用法提示 + 品牌）。
  **序号由调用方给定，渲染器自己不编号** —— 发卡片的人和收数字的人必须共用一个号。
  分组行用**描边**序号以区别于模型行的实心序号；分组的组头不再只是装饰，它是一个可选序号。
  主题色沿用插件既有的粉→紫（`THEMES["rose"]`），可换 `indigo` / `teal`。
  列间距从 30 加到 44：太窄时「`-`」会和下一个数读成一体（看起来像 `-0.0%`）。
  行内胶囊改画在**名字那一行**的高度上：画在行中央会和下面那行副标题叠在一起。
- **`_grouped_card_rows` 一次性产出「卡片行表 + 选单选项表」**：`options[i]["index"] == i + 1`，
  模型行与分组行共用同一个数字空间（① 供应商、② 它下面的模型、③ …），
  所以「回组号 = 选整组」是天然的，不需要用户先分辨这个号是组还是模型。
  排序改成**一律按名字**（供应商名、模型名各自升序），刷新两次顺序不会变。
- **选单机制升级**：`_PICKERS` 里存的从「一串 pid」改成「选项表」`{label, pids, group}`，
  新增 `multi` 标记；`_picker_take_multi` 支持一次领多个（越界**不作废**，用户可重输），
  旧的 `_picker_take` 保留为单选取的第一项。
- **`NumberPickerFilter` 放宽到「一串序号」**：`3` / `1 3` / `1,3` / `1、3` 都认，
  但仍要求**整条消息只有数字与分隔符**（发 `3楼见`、`12345` 一律不吞，日常聊天照旧走原流程），
  并且一条消息最多 8 个序号 —— 不吞用户的购物清单。
- **`/模型状态`**：发通用卡片（只读、不挂选单）。分组 + 序号 + 最近一次延迟 + 成功率；
  「默认模型」从第二行小字改成名字右侧的胶囊 + 行高亮（切模型时最要紧的就是认清当前那个）。
- **`/模型统计`**：不带参数发同一张卡并挂单选选单，回序号出**明细卡**（首字 / 首字 P95 / 整轮 /
  成功率 / 失败次数）；带关键词仍然一步直出。
- **`/检测模型` → `/模型检测`**（旧名注册为 `alias`，老习惯不用改）：
  不带参数发带序号卡片，**回序号可多选**（`2 4`），**回分组序号 = 测整组**；
  一次最多真打 `_PROBE_MAX_PICK = 10` 个（分组可能很大，得有闸）；关键词路径不变（单次 3 个上限）。
  结果卡片改用 `_card_png`，带序号与「成功 N / 失败 M」结论。
- **`/切换系统模型`**：改用通用卡片（全部有效对话模型、不按检测通道裁剪），
  当前默认那行带「默认」胶囊；回数字走 `async for`（修掉第 6 条），选单改为单选、
  回多个数字会提示「只回一个」且**不作废**。
- **`/模型静音` 改为全局开关**：`/模型静音`（切换）/ `开` / `关` / `状态`，
  落到插件配置 `alert_notify_enabled` —— 与 WebUI 上「把告警主动发给管理员」是同一个值，
  指令与 WebUI 互相看得见、重启也不丢；`save_config()` 失败会**回滚内存里的值**并说明原因
  （否则会出现「本进程已静音、磁盘上没改、重启后又是另一个样」）。逐模型静音与其时长解析一并下线
  （`model_state.muted_until` 保留，将来要在 WebUI 上做单模型静音时是现成的）。
- **`_dispatch_alerts` 增加全局静音闸**：静音期间**补发也一并停掉** ——
  只堵新增、放任补发的话，按了静音仍会被历史事件刷屏，等于没静音；
  静音期间不写 `notified_at`，所以取消静音后 `pending_alerts` 会把它们补上，不丢。
- 删除重复定义的 `cmd_model_probe`（保留的那份改成 `/模型检测`）、删掉随之无用的
  `_STATE_ORDER` 与 `_parse_duration`；`_rows_text` 里补回状态词（纯文本没有颜色，不写状态就看不出谁坏了）。

### 自检

- 新增 `tests/test_card_rows.py`：对着 `main.py` 的真代码断言「卡片第 i 行的序号 == 选项表第 i 项的
  index == i + 1」、分组序号摊平后等于组内全部 pid、分组与组内都按名字排序、明细卡不带序号。
  这一条是本次改动的核心不变量，而且错位时**不报任何错**（图看着好好的），必须机器盯着。
- `tests/test_picker_flow.py` 补「选项表 / 分组 / 多选 / 序号解析 / filter 多选放行」用例；
  改为从 `main.py` 里连**模块级常量**（`_MAX_PICKS`、两个正则）一起抠出来，避免测试里另抄一份公式。
- `tests/test_static_guards.py` 指令清单更新为「模型检测」，并新增**别名检查**：
  改名的指令必须留住旧名，否则老用户手里的指令会静默失效。

## v1.3.9

补上核心统计表结构性看不见的失败（自己埋点），指令侧加序号选单与默认模型切换，监测窗口默认「今天」。

### 问题

1. **实时监测在骗人**：AstrBot 日志里某个模型当天失败了好几次，面板上却写着 100% 成功。
   用户原话：「就是 llm 每次调用成功和时间、延迟你好像都没记录上去的感觉」。
2. `/检测模型` 必须打对模型名，而 provider 名字根本记不住。
3. 想在聊天里换系统默认模型，只能去后台配置。
4. 默认窗口是近 7 天，今天的故障被七天的分母摊平掉了。

### 根因（第 1 条是结构性的，不是 bug）

核心只有 `_record_internal_agent_stats`（`pipeline/process_stage/method/agent_sub_stages/internal.py:553`）
这一个写入方，**一轮对话记一行**，`provider_id` 取当时的 `self.provider`；
而 `core/agent/runners/tool_loop_agent_runner.py` 在候选循环里会 `self.provider = candidate`
（约 569 行）逐个试。于是**主模型失败、备用救回来**的那一轮，落库的是备用的成功行，
主模型那次失败在 `provider_stats` 里根本不存在。

`on_llm_response` 钩子在模型报错时**不触发**（runner 合成 `role="err"` 分片后直接 return），
所以「失败」这一半在核心侧拿不到第二个来源 —— 只能自己埋点，没有第二条路。

### 修复（后端）

- 新增 **`call_recorder.py`**：在**类**上包装 `text_chat` / `text_chat_stream`。
  两个要点：只看 `cls.__dict__` 里本类自己的实现（顺着 MRO 拿基类方法再 setattr，
  子类覆写的那份永远拦不到）；`_model_panel_wrapped` 标记保证幂等，可反复 install 而不套娃。
- 新表 **`llm_calls`**（一次尝试一行：`ts/day/provider_id/provider_model/ok/aborted/streamed/latency_ms/ttft_ms/error_code/error_message`）
  加三个索引与 `cleanup_calls_older_than`。核心那张表没有保留期清理，这张自己清。
- 插件自己的探测包在 `suppressed()` 里：结果已经写进 `model_test_results`，
  不抑制的话每轮探测会再往 `llm_calls` 记一行，同一件事被数两遍。
- `monitor.evaluate` 加 `_field()`，同时吃核心的 `CallRecord` 与本表的 dict 行 ——
  状态机只留一份实现，两边各写一套必然分叉。
- 巡检游标拆成 `calls_cursor` / `stats_cursor`：换数据源时若共用一个游标，
  会把另一张表的老历史当成新数据重放一遍并立刻告警。
- 告警数据源：`llm_calls` 一旦有数据就用它，否则回落 `provider_stats`。
- `/检测模型` **不带参数**改为列序号选单；新增 **`/切换系统模型`**（列全部有效对话模型 + 今天的状态与延迟，回数字切换）。
- `NumberPickerFilter`：「整条消息就是一个数字」且这个 **(会话, 人)** 挂着未过期选单才放行。
  两条都必须在 filter 阶段判完 —— 进了 handler 再决定不管，用户日常发的一句「3」就被插件吃掉了，
  那是最难排查的投诉。按人不只按会话，否则管理员刚列完表，群里别人回个数字就把默认模型切走了。
- `_actor_key` 兜底核心的已知行为：`get_sender_id()` 在 `user_id` 是 int 时**直接返回空串**。
- 「今天」折算成滚动小数天（`_today_days`），复用全部现有窗口逻辑，
  改动面从三个文件缩到一个函数；`api_health` 缺省即今天。

### 修复（前端）

- 实时监测新增 **「逐次成败」「逐次延迟」** 两列，悬停给「共 N 次 / 成功 / 失败 / 打断」与最近一次失败错误码。
- 窗口选项前置 **「今天」** 并设为默认（`DAY_OPTIONS` 允许 `"today"` 与数字混排）。
- 口径说明补第 4 条：为什么「成功率 100% 而逐次有失败」不是矛盾 —— 是备用起作用了。
- `calls_available === false` 时给一条横幅说明这层埋点从本次启动才开始记录，重启前的历史补不回来。

### 已知限制

- 包装在插件装载时安装，本仓库**没有 `importlib.reload` 热重载列表**，
  所以从旧版本热更上来不会自动装上，**必须完整重启 AstrBot**。
- 不走 `text_chat` / `text_chat_stream` 的调用路径（极少数插件自定义 provider 方法）埋不到。

### 自检

- 新增 `tests/test_call_recorder.py`：真异步生成器，覆盖成功 / 抛错 / `role="err"` 软失败 /
  ttft / 流中途抛错 / 消费方提前 `aclose` / install 幂等 / 子类覆写 / 抑制 / 存储写失败不影响对话。
  抓到过一个真 bug：`if ttft` 把合法的 0 当成「没测到」丢掉，整列首字延迟会空。
- 新增 `tests/test_monitor_alerts.py`：两种行形状等价、**被备用救回仍报故障**、
  aborted 中性、连续成功 2 次才算恢复、冷却、阈值。
- 新增 `tests/test_picker_flow.py`：用 AST 把选单函数从 `main.py` **原样抠出来** exec
  （main.py 依赖整个 astrbot 运行时，本机 import 不了），测领取即作废、越界不作废、
  过期清理、按人隔离、filter 放行条件、`_actor_key` 兜底。
  反向对照：把 `index < 1` 改成 `index < 0` 后立刻 MISS 4 项，确认它不是空跑。
- 前端：桩桥 + headless Chrome 截图验收「成功率 100% / 逐次失败 3 次」并排显示；
  把 `calls_available` 改成 false 验证横幅确实出现（`v-else-if` 链没被上面的告警吃掉）。

## v1.3.8

补齐计费维度，并让单价/倍率真的算出钱来。

### 为什么

计费字段此前**全仓没有任何地方使用**，只是存着 —— 倍率不乘任何东西就等于一个备注数字。
这轮把 `llm_usage`（已有每次调用的 input/cached/output token 与请求数）和档案单价接上，
让「填价」有回报。

### 计费类型与字段

- `BILLING_TYPES` 新增 **`per_request`（按次计费）**。它和 token 计价是两套单位，
  所以单价字段分开：token 类填「元/百万 token」，按次类填「元/次」，
  后缀在界面上分别标出来 —— 混填会差六个数量级。
- 模型级新增 `price_per_call`、`rate_multiplier`（NULL = 跟随分组）。
- 新增 **`vendor_profile` 表**：分组级的 `currency` / `rate_multiplier` /
  `daily_call_limit` / `note`。倍率与每日限额天然是一家一个值，
  逐个模型填十遍既累又容易填得不一致（用户原话「倍率一般是整个分组都是这个倍率」）。
  生效优先级：**模型覆盖 → 分组 → 1.0**，币种同理；抽屉里实时回显最终倍率，
  「留空跟随分组」不是一句空话。
- 老库靠 `_migrate()` 的 `ALTER TABLE` 补列，否则老用户一进档案页就 "no such column"。

### 预估花费

`estimate_cost()`：
- token 类按 AstrBot 的 usage 拆法计 —— `input_other` 与 `input_cached` **互斥**
  （`total = input + cached + output`），所以缓存部分单独按缓存价计，不能混进 input 重复收费；
  缓存价没填时**退回输入价而不是当 0**，当 0 会系统性低估成本。
- 按次类 = 请求数 × 单次单价 × 倍率。
- **估不出来返回 None，不返回 0.0**。前端显示 `未填单价` 而不是 `¥0.00` ——
  后者会被读成「这个模型真没花钱」。`fmtMoney` 里 0 单独走两位小数，
  避免免费模型显示成 `¥0.0000` 这种排版事故。
- 首页给今日 / 近 7 天总额，并明说「另有多少个模型未填单价」，
  否则那个数会被当成全部支出。模型行给各自金额与生效倍率。

### 每日次数限制（只展示不拦截）

组头显示 `421 / 500`，用到八成变橙、超了变红。刻意不做拦截：这是供应商侧的策略，
我们既没有权威计数也没有拒绝义务，拦错了会伤到正在服务用户的模型。
**口径要写清**：数字只来自 `llm_usage`（`on_llm_response` 写的真实对话），
不含定时探测与手动检测，所以它是供应商计数器的**下限**而不是精确值。

### 顺手修掉的死链

首页「前往检测」还指着 v1.3.5 就删掉的 `#/detect`，点了直接白屏。字符串链接删路由时
不会编译报错，所以给 `tests/test_webui_static.py` 加了第 6 项检查：
`href="#/..."` 与导航项的 `to` 必须命中 `main.ts` 里真实注册的路由。

### 验证

- 新增 `tests/test_billing.py`：`estimate_cost` 逐条盯具体金额（缓存互斥、缓存价缺省、
  倍率 0 合法、负倍率不返回负数、没填价返回 None）、`resolve_pricing` 三级优先、
  分组档案 DAO、老库迁移补列、用量聚合。
  另加**后端枚举 ↔ 前端 i18n 标签双向对齐**检查 —— 加计费类型只改后端的话，
  下拉会显示成裸键 `monitor.billing.per_request` 且不报错，正是「构建成功、界面是坏的」那一类。
- `tests/_aiosqlite_stub.py`：把原来内联在 scope 测试里的 aiosqlite 薄壳抽出来共用，
  避免两份各自漂移。
- 反向对照：把缓存价缺省改成当 0 算 → 该断言立刻 MISS；把死链改回 `#/detect` → 路由检查抓到。
- 合并页与首页在桩桥 + headless Chrome 下真跑过：分组倍率/限额录入 → 草稿 →
  `POST /panel/vendor` 载荷核对、保存后组头 `12 / 200` 重算、限额变色、
  预估花费列（`¥7.30` / `$9.00` 模型级币种覆盖 / `未填单价` / `¥0.00`）、
  倍率标签区分「跟随分组 ×1.2」与「倍率 ×2.5」、抽屉新增两个字段与单位后缀、
  首页花费条。四个测试脚本全绿。

## v1.3.7

卡片按供应商分组，脚注收成一句。

### 分组

`/模型状态` 与 `/模型统计` 的行改为**按供应商分组**：每组一条组头（供应商名 + 模型数），
组内一行一个模型。上一版只写模型名，而好几家渠道都供同一个 `deepseek-v3`，
不带分组根本分不清是哪一路 —— 用户原话「同名的模型我都搞不清楚」。

- 组间按「组内最坏状态」排序，出问题的供应商整体浮到上面；组内仍按状态优先。
- 组头已经写了供应商，组内就只留模型名，不再重复；`_card_name` 那套
  「重名才带供应商标」的启发式随之删掉 —— 分组本身就是更明确的表达。
- 渲染器加通用分组能力：行带 `group` 字段就画组头，不带就照旧平铺
  （探测结果卡仍是平铺，它最多 3 行且名字本来就带供应商）。
- 文本降级（没字体时）同步输出 `— 供应商 —` 分组行。

### 脚注收敛

卡片是发到聊天里的，之前底部那几行把内部判定全写了出来
（「只列开放了「指令检测」通道的模型（与 /检测模型 可测的范围一致）」
「成功率合并所有调用来源（对话 / 手动检测 / 指令检测 / 定时探测）」）。
现在只留一句「延迟为最近一次调用耗时」+ 时间；告警卡也各留一句。

顺带把错误码说人话：卡片上不再出现 `rate_limit` / `timeout` 这种内部码，
统一走 `_error_label` → 超时 / 请求受限 / 拒绝连接 / 鉴权失败 / 模型不存在 / 服务异常。

### 验证

- 真出图看过：分组带副标题 / 分组纯单行 / 平铺探测卡 / 告警卡 / 五列明细卡五种形态。
- `_error_label` 逐个码打点，未知码与空值都落到「异常」而不是裸码。
- `tests/test_static_guards.py`、`tests/test_storage_scopes.py`、`tests/test_webui_static.py` 全绿。

## v1.3.6

用户看完 v1.3.5 的卡片说「有点丑、不清爽」，这轮按反馈重排，并统一延迟口径。

### 卡片排版：一行一条，去掉斑马墙

- **不再自动折两栏**。上一版为了压住「列全部模型」的高度做了两栏，实际效果是窄栏里
  模型名被截得看不出差别，而且读的时候要来回扫，比长列表更累。
- **数据行去掉底色药丸**。粉彩药丸适合「标签：值」的资料条目，不适合右对齐的数字表——
  一行一个药丸叠上去就是一堵斑马墙。换成状态色点（带一圈淡晕）+ 发丝分隔线 + 留白。
- 标题从「整块粉紫渐变底」改成白底 + 下方一条 3px 粉→紫细线，汇总从胶囊盒改成
  一行「色点 + 标签 + 数字」。整张卡只保留那一条渐变作为装饰。
- 卡片宽度改成**跟着内容自适应**（760 起，上限 1080）：明细卡有五个数值列，
  固定 760 会把模型名挤成「deepseek-v3 …」。名称列至少留 260px。
- 踩到一个 Pillow 的坑并写进模块注释：往 RGBA 图上画带 alpha 的颜色是**替换像素**
  而不是叠加，白卡上画 `(*color, 46)` 会得到一个近乎透明的窟窿。淡色必须先用
  `_mix` 和白底混成不透明色。

### 展示范围与延迟口径

- `/模型状态`、`/模型统计` **只列开放了「指令检测」通道的模型**（`_command_scope`）。
  理由很直接：卡片列了 12 个、`/检测模型` 却说其中 9 个没开放通道，两边对不齐最让人困惑。
  顶部汇总计数跟着重算，一个都没开时不再发空卡，而是回一句「去哪勾」。
- 这两条查询指令改为 **管理员限定**，与 `/检测模型`、`/模型静音` 同一受众。
  `/陪伴分数` 保持全员。
- **延迟一律按毫秒原样显示**（`4200ms` 而不是 `4.2s`），只有超过 1 分钟才折成 `1分5秒`，
  再往上 `1时2分`。看延迟的人要比的正是那几百毫秒，折算成带一位小数的秒等于把精度
  四舍五入掉了。后端 `_fmt_ms` 与前端 `fmtMs` **同一条规则改掉**——
  面板显示 4.2s、卡片显示 4200ms 会让人怀疑两处不是同一个数。
- 实时监测页的模型列同样去掉供应商标前缀（组头已有），模型名加粗，与合并页一致。

### 验证

- 卡片用真 Pillow 出图看过 6 行 / 22 行 / 告警 / 五列明细 / 探测结果五种形态，
  明细卡自适应宽度后模型名不再被截断。
- `_fmt_ms` 打点验证边界：`870ms / 4200ms / 59999ms / 1分0秒 / 1分5秒 / 1时2分`，
  以及 `0`、`None` 仍显示 `-`（不能显示成 0ms）。
- 新增 `tests/test_webui_static.py`（纯解析 .vue 文本，不需要 node）：模板 `<n-xxx>`
  是否 import、`t("a.b")` 键是否存在、动态拼接键按 DYNAMIC 表补齐、import 未使用。
  反向对照注入漏 import 组件与假 i18n 键，两项都被抓出。
- `tests/test_static_guards.py` 的指令权限期望同步改成四条 ADMIN，跑通。
- 合并页与实时监测页在桩桥 + headless Chrome 下重新截图确认（毫秒显示、模型名不带供应商）。

## v1.3.5

按用户反馈合并页面、重做卡片观感，顺带挖出两个存储层的真 bug。

### 模型检测 + 模型档案 → 「模型管理」一页

原来两页配的是**同一张表的同一批字段**（`detection_preferences` 的 `enabled` 就是
`scope_manual`），分成两页只会让人在两个地方找同一个开关。合并成新页 `ModelsView.vue`，
`DetectView.vue` / `ProfileView.vue` 删除，路由收敛为 `/models`。

- 三通道**各占一列并把列名写全**（手动检测 / 定时巡检 / 指令检测）。之前挤成一列
  「三通道」时只能靠 tooltip 猜哪个开关是谁。
- 组头给「本组计费」下拉（选完即作用到整组）和三个通道芯片（写着 `4/5` 这种当前
  参与数，点一下整组翻转）。放在组头而不是表头，是因为表头是所有组共用的列定义。
- 模型名**加粗**，第二行只放**备注**；供应商名组头已经有了，行内不再重复一遍。
- 检测前自动先保存草稿：否则「刚把某模型的手动检测打开就点一键检测」会按库里的旧值
  把它跳过——屏幕上的样子必须就是后端真正用的那份。
- 去掉了检测勾选的 localStorage 双写，勾选偏好只留后端一处真相。
- **操作列不再 `fixed: "right"`**：钉住之后，1366 宽度下它会把「指令检测」整列盖住，
  看着像这一列消失了。宁可留一条可见的横向滚动条，也不要静默藏掉用户点名要看的列。

### 卡片改浅色粉彩，`/模型状态` 列全部模型

- `card_render.py` 从深色监控面板改成浅色粉彩（白卡体 + 粉→紫渐变头 + 行内圆角药丸 +
  带色柔光投影 + 品牌描边）。观感借鉴「萌萌资料卡」，配色与内容组织仍是监控面板那套，
  没有照抄它的代码。深色截图塞进 QQ 气泡会发灰、对比被吃掉，浅色更耐看。
- 状态色整体压深一档，否则在浅底上压不住。
- `/模型状态` **去掉 12 行上限**（`CARD_MAX_ROWS` 删除）：这张卡的意义就是「一眼看完
  谁挂了」，截断会把恰好故障的那个藏起来。超过约 15 行由渲染器自动折两栏，
  单行行高压到 44px。
- 砍掉冗余细节：不再显示 `n=xx` 样本数、「平均首字」「最新结果」这类次要列，
  总览只留 **延迟 + 成功率**；第二行只留给故障原因 / 限时免费剩余 / 静音中 / 默认模型。
- 模型名默认只写模型本身，**只有同名模型出现在多个渠道时**才带供应商标 ——
  一长串「供应商/路径」在两栏里会被截得看不出差别。
- 脚注改为**按像素折行**而不是截断：口径说明被截掉就等于没写。
- 明细卡（`/模型统计`）此前在只有 1 行时不画表头，五个裸数字没有标签；现在只要有列就画。

### 顺手修掉的两个存储层 bug

1. **`/panel/scope` 对 manual 通道传 `null`（前端「跟随默认」）会抛
   `IntegrityError: NOT NULL constraint failed: detection_preferences.enabled`。**
   `manual` 复用老表的 `enabled` 列，它是 `NOT NULL`，落不回 NULL。而 manual 的默认
   本来就只有「开」，所以写入侧把 `None` 归一成 `1`。第三态只对 scheduled / command
   成立，前端据此只在定时列保留「跟随默认」按钮。
   —— 由 `tests/test_storage_scopes.py` 第一次跑出来，不是靠读代码读出来的。
2. **只标过计费、没碰过通道开关的模型，会整个从检测范围结果里消失。**
   `get_detect_scopes` 原先从 `detection_preferences` 单侧 LEFT JOIN `model_profile`，
   前者没有行就什么都不返回。而定时探测名单和面板显示读的都是这个函数，结果就是
   「免费模型默认参与定时巡检」这条推导对它根本不生效 —— 免费模型永远排不进巡检。
   改成两表 `UNION` 出 provider 全集再左连。

### 验证

- 新增 `tests/test_storage_scopes.py`：用标准库 sqlite3 写了个 aiosqlite 薄壳
  （本机没装 aiosqlite；壳只做异步转接，NULL 与 SQL 原样交给 sqlite3，所以测的是真表结构），
  覆盖三通道 NULL/显式语义、批量写不清空其它列、计费推导定时默认、档案行不被漏掉。
  反向对照：把并集遍历改回单侧 JOIN → 4 项 MISS；去掉 `None→1` 归一 → 直接抛
  IntegrityError。两处修复都已还原验证。
- 前端静态自检（模板 `<n-xxx>` 是否有 import、`t("a.b")` 键是否存在、动态拼接键是否补齐、
  import 未使用）全仓 7 视图 + App.vue 通过；反向对照注入漏 import 组件与假 i18n 键，两项都被抓出。
- 合并页在桩桥 + headless Chrome 下真跑过：分组/组头芯片翻转→草稿→`/panel/scope` 载荷逐条核对、
  一键检测流式进度与统计、详情弹窗、档案抽屉、本组计费下拉、1500 与 1366 两档宽度截图。
  卡片用真 Pillow 出图看过 8 行 / 30 行两栏 / 空列表 / 告警 / 明细五种形态。
- `python tests/test_static_guards.py` 五条指令注册与 `astrbot.*` 导入解析全通过。

## v1.3.4

补文档时挖出一个静默失效的指令。

### 问题

`/模型静音` 在 CHANGELOG、README 和功能描述里都被当成可用特性写着，实际**从未注册过**——
在聊天里发它，bot 完全无反应，也没有任何报错日志。

### 根因

M5 那一版给 `main.py` 插入代码时，用 `cmd_model_mute` 头上的两行装饰器当了
`old_string` 的上下文，替换文本里没有把它们重新写回去。于是：

- 方法体还在，`python -m compileall` 通过，导入期不报错；
- 少了 `@astr_filter.command(...)` / `@astr_filter.permission_type(...)`，
  AstrBot 扫描指令时看不见它，它就退化成一个普通的死方法；
- 丢装饰器是**静默**的，不像丢导入会炸——所以 190 多条断言全绿也没发现。

写 README「指令有哪些」时，我用 grep 逐个枚举注册了的指令，才对不上数量。

### 修复（后端）

- 把 `@astr_filter.permission_type(ADMIN)` + `@astr_filter.command("模型静音")` 补回
  `cmd_model_mute`，顺序保持 permission_type 在上。
- 新增 `tests/test_static_guards.py`（纯解析源码，不需要 AstrBot 环境、不给外部 API 打桩），
  跑 `python tests/test_static_guards.py` 即可。它管两类静默失效：
  - **指令注册**：五个 `cmd_*` 必须带 `@command`、`permission_type` 要写在 `@command`
    之上（AstrBot 由外向内套装饰器，写反了权限不生效）、必须是 async generator，
    另外断言 `@register` 没误挂到顶层函数上。
  - **`astrbot.*` 导入**：顺着核心源码把 `from x import *` 的门面链展开，逐个核对
    名字是否真的存在。需要本地有 `_Refs/AstrBot`，找不到就 skip 而不是 fail。
- 反向对照做过，不是「空跑全绿」：手工把 `/模型静音` 的装饰器删回去、把
  `permission_type` 挪到 `@command` 下面、把那年的
  `from astrbot.api.message_components import MessageChain` 原样重放一遍、
  再凭空捏一个不存在的名——四项分别被抓住，基线仍为 0 失败。
  对照脚本是一次性的，用完已删。

### 文档

- README 新增「指令」一节：先给一张权限 / 是否花额度的总表，再逐条写用法、参数、
  匹配上限和被拒绝时的行为。数字全部回查过代码而不是凭印象写：
  卡片最多 12 行（`CARD_MAX_ROWS`）、`/检测模型` 单次上限 3 个（`_PROBE_MAX_TARGETS`，
  正好 3 个放行）、`/模型统计` 超过 6 个命中改为提示收窄、静音默认 2h
  （`_parse_duration(...) or 7200`）。
- README 功能清单补齐 v1.3.0 之后的四块：实时监测、模型档案、自动告警、定时探测。
- 明确写出**前置条件：管理员 ID**。`/检测模型`、`/模型静音` 和所有告警投递都依赖它，
  而默认值 `astrbot` 是占位符、插件会主动跳过——不填就是「指令说没权限、告警一条收不到」，
  这一条以前只写在代码注释里，用户侧无从得知。

## v1.3.3

按用户纠正重做「实时监测」，并把监测页与档案页改成分组铺满一页。

### 实时监测的理解纠正

- 上一版把实时监测做成了**只看用户对话**（只读核心 `provider_stats`）。
  实际要的是**每一次 LLM 调用**：用户/群对话、WebUI 手动检测、指令检测、定时探测都算。
- 新增 `_merge_ledger`：把两个互不相通的记录处合并成每个模型的调用台账——
  真实对话在核心 `provider_stats`，三种探测在自己的 `model_test_results`。
  只读前者会出现「刚手动测过并且失败了，面板却显示正常」。
- **默认展示最新一次调用结果**（判定 + 延迟 + 来源标签 + 错误码 + 时刻），
  窗口平均值退到次要列。来源标签按 `test_sessions.trigger` 映射
  （single/all/stream → 手动，command → 指令检测，scheduled → 定时探测）。
- **成功率合并所有来源，延迟不跨来源合并**：探测是空载一句 PONG 的往返，
  和真实对话差一个数量级，平均到一起就没有意义了。所以「平均首字」只统计对话、
  「探测延迟」单独一列，展开行里再给来源构成。
- 健康态判定因此也吃合并后的数字：只看对话路失败率 50% 的模型，
  加上一次失败的指令检测就是 67%，才把真实状况暴露出来。

### 展示方式

- 监测页与档案页**都去掉分页**，改为按供应商分组、每组一张表铺满一页
  （借「模型检测」页的分组结构，但没有照抄：组头带该组的故障/降级/正常计数，
  组间按「最坏模型」排序，出问题的供应商自动浮上来）。
  理由：换模型是「扫一眼哪家不对」的活，分页会把故障模型藏到第二页。
- 档案页组头新增「本组设为所选计费」，配合原有的按供应商下拉批量，
  批量标注不用再跨页操作。

### 其它

- `storage` 新增 `probe_window`（带 `trigger` 的探测明细查询）；
  视图不再调 `latest_per_provider`，少一次白查。
- 窗口内一次调用都没有时成功率返回 `null` 显示 `–`，不是 0% 也不是 100%。
- 卡片脚注同步改成新口径文案（原来还在讲已经去掉的「整轮」列）。
- 顺带把 `ProfileView.vue` 的换行统一成 LF（该文件此前是 CRLF/LF 混合，
  git 标记 `w/mixed`，与仓库内 `api.ts` 等 LF 文件不一致）。

### 验证

- 台账合并 18 项断言：来源合并/打断排除/成功率合并/延迟分列/token 只算对话/
  最新一条跨来源取时间最大/探测来源映射/空窗口为 null/纯探测模型也能进状态机。
- 浏览器冒烟（桩数据按新结构造）：两页各 3 个供应商分组、**分页控件数为 0**、
  最新结果列显示「失败 – ·对话 auth」这类组合、组头批量按钮在未选计费类型时禁用、
  无 i18n 键泄漏。
- 卡片端到端出图并人工查看，确认列头已是「最新结果 / 平均首字 / 成功率 / 样本」。


## v1.3.2

- 修复 **v1.3.1 装不上**：`from astrbot.api.message_components import MessageChain`
  直接抛 `ImportError`，插件加载失败、安装被回滚。
  - 根因：`astrbot/api/message_components.py` 只有一行
    `from astrbot.core.message.components import *`，而 `MessageChain` 定义在
    `astrbot/core/message/message_event_result.py`，**不在 components 里**，
    所以 `import *` 也带不出来。它由 `astrbot.api.event` 导出。
  - 修复：改成 `from astrbot.api.event import AstrMessageEvent, MessageChain, filter as astr_filter`，
    `Image` / `Plain` 仍从 `astrbot.api.message_components` 导入（那两个确实在 components 里）。
  - 为什么本地 190+ 项断言全过却没发现：测试脚本用 `sys.modules` 给 astrbot 打桩，
    桩模块里我写 `MessageChain=...` 它就存在，**结构上不可能发现真实导入路径写错**。
    这类错只在真装上 AstrBot 的那一刻暴露。
  - 防复发：补了一个静态检查，按 AST 解析真实核心源码（不执行任何代码），
    把插件里每条 `from astrbot... import X` 的 X 逐个对照模块公开面
    （`__all__` 优先，并展开 `import *` 再导出链、并入包目录下的子模块名）。
    反向对照验证过：它能抓到 v1.3.1 这个 `MessageChain` 错误本身。
  - 另把属性级调用点也逐个对过源码：`filter.PermissionType`、`context.send_message`、
    `context.get_db`、`context.platform_manager.platform_insts`、
    `event.chain_result`、`provider.test(timeout=)`、`provider.text_chat_stream(prompt=)`、
    `Image.fromBytes` 均与 4.28.1 实现一致。


## v1.3.1

新增「模型监测 + 模型档案 + 自动告警」整条链路。设计与口径推导见 `docs/模型监测与配置规划.md`。

### 为什么不是自己埋点

原本计划给每次对话加计时钩子。查核心源码后发现没必要：AstrBot 4.28.1 已经在
`provider_stats` 表里按轮记录了 `start_time` / `end_time` / `time_to_first_token` /
`status`（写入点 `agent_sub_stages/internal.py`，dashboard 的 `stat_service` 已经在聚合它）。
所以「记录每次调用的状态和耗时」从**写埋点**降级成**读一张别人的表**，零成本、零侵入。

同时发现一条硬约束：`on_llm_response` 钩子**在模型报错时根本不触发**
（runner 合成 `role="err"` 后直接 return，不走 `on_agent_done`）。
也就是说 v1.2.x 那个记 token 的钩子对失败调用是完全瞎的，
而核心也没有 LLM 错误专用钩子（`OnPluginErrorEvent` 只管插件 handler 异常）。
→ 「报错就告警」这一半只能建立在读 `provider_stats.status="error"` 上，没有第二条路。

### 新增（后端）

- `stats_reader.py`：核心 `provider_stats` 只读适配层。只用裸 SQL，不 import 核心 ORM 模型，
  核心挪动类定义不打断这里；表读不到（老版本 AstrBot）一律静默降级，绝不影响对话。
  - 刻意用字符串比较过滤 `created_at`：该列是 SQLAlchemy 写的定长 UTC 字符串，
    绑 `datetime` 对象给 DBAPI 会因为分隔符不一致**静默查出 0 行**。
  - 首轮把游标放到表尾，否则刚装上就会因为陈年历史里的失败发一条莫名告警。
- `monitor.py`：告警规则引擎，纯函数、不碰 IO，可离线断言。
  三条纪律：连续失败才算故障（一次抖动就告警会让人开始忽略告警）；
  `aborted` 不改变任何计数器；恢复需连续成功 2 次。
  冷却同时覆盖故障与恢复两类——只给故障设冷却会留刷屏缺口（阈值附近抖动时
  down→恢复→down→恢复 能无限发恢复）。
- `card_render.py`：Pillow 卡片渲染。**零新增依赖**（核心自身依赖 `pillow>=11.2.1`）。
  刻意不用 emoji 与 fontTools（不是核心依赖），状态用绘制色条表达；
  中文字体只探测系统字体，不往包里塞几十 MB 字体，探测不到就退回纯文本——
  宁可丑，不可发一张全是豆腐块的图。渲染走 `asyncio.to_thread`，不在事件循环里画。
- `storage.py`：新增 `model_profile`（计费类型 / 限时免费到期 / 每百万 token 单价 /
  来源渠道 / 角色 / 流式能力 / 探测模式 / 备注）、`model_state`（告警状态机宿主）、
  `alert_events`（可追溯 + 冷却依据）、`panel_state`（增量游标）；
  `detection_preferences` 加 `allow_scheduled` / `allow_command` 两列，存 NULL 表示
  「跟随计费类型推导」——付费模型默认不进定时巡检，避免定时任务烧真金白银。
  老库用 `ALTER` 补列（`CREATE TABLE IF NOT EXISTS` 不会给已存在的表加列，
  漏了老用户一进档案页就 `no such column` 白屏）。
- 新增 `GET /panel/health`、`POST /panel/profile`、`POST /panel/scope`。
  写入前用已加载的 provider id 校验，拒绝野 id。
- 新增 `reap_orphans`：回收 provider 被删除/改名遗留的孤儿行
  （AstrBot `delete_provider` 不做任何级联清理，且 id 用户可编辑）。
- 后台巡检循环：`initialize()` 起任务、`terminate()` 显式取消。
  单轮异常不终止循环——「没告警」看起来永远像「一切正常」。
  `terminate` 必须定义在本类里，热重载才会调到（核心查的是类 `__dict__`）。
- 投递失败可重试：未送达的告警事件保持 `open` 且 `notified_at` 为空，下一轮补发，
  超 24 小时放弃。刚装上时 `admins_id` 往往还是默认占位值 `astrbot`，
  不重试的话第一批告警会被永久吞掉。
- 告警文案统一脱敏（sk- 密钥 / Bearer 令牌 / URL 内凭据 / key=value）。
  **规则顺序有讲究**：通用 `key=value` 若先跑，会把 `Authorization: Bearer xxx` 里的
  `Bearer` 一词吃掉，真正的令牌值反而逃过脱敏。
- 定时探测（默认关闭）+ 四道成本护栏：每日调用预算硬闸、并发上限（钳 1~8，
  并发一高会自己触发 429 把健康模型判死）、付费类重试强制 0、撞手动检测时让路顺延。
  探测结果包成 `CallRecord` 复用同一个规则引擎，避免两套判定互相打脸。
- 流式探测：TTFT 口径与核心对齐（第一个 `is_chunk` 分片的到达耗时），
  拿到首字立刻断流并**显式 `aclose()`**——只靠 `break` 的话收尾依赖 GC 回调，
  这条链路每几分钟跑一轮 × N 个模型，漏一次攒一个僵尸 HTTP 连接。

### 新增（指令）

- `/模型状态`、`/模型统计 <关键词>`：只读，不请求模型、不产生费用。
- `/检测模型 <关键词…>`：管理员限定，真打模型。先回执再后台推卡片
  （3 个模型串起来最坏 3×超时，直接在 handler 里等会让用户以为 bot 挂了）。
  超上限时列出命中项提示收窄，而不是静默只测前 3 个。
- `/模型静音 <名称> [时长]` / `/模型静音 取消 <名称>`：只关通知不关采集。

### 新增（前端）

- 「实时监测」页：并排展示真实对话监测与最近探测，脚注固定写明口径；
  没测到的值显示 `–` 而不是 0（非流式调用压根不产出 TTFT，显示 0 会让人以为模型快得离谱）；
  样本 n<3 淡化；排序 故障>降级>无数据>健康（「没测过」比「已知正常」更该被看见）。
- 「模型档案」页：表格放高频字段，低频进抽屉；三通道用虚线框区分「跟随默认」与「显式设置」；
  草稿制编辑 + 粘性保存条；按供应商批量设计费类型。
- 文案只做中文（`fallbackLocale` 已是 zh，新键只加 `zh.json`）。

### 修复

- `set_detection_preferences` 由「先 DELETE 全表再插入」改为逐条 upsert。
  原写法会把新增的两个通道一并抹掉：用户在档案页配好的定时名单，
  一次普通的 WebUI 勾选保存就静默清空。
- 健康态判定统一：巡检持久状态优先于按需现算的近似值。两边各给结论而不统一的话，
  面板显示的和告警发出去的会各说各话。

### 说明

- 探测延迟（空载一句 PONG 的往返）与真实对话延迟**不可横向比较、不可合并平均**。
  这是本版本所有界面与文案的硬约束，卡片脚注里也写死了。
- 定时探测默认关闭且新装用户名单为空（`unknown` 计费不满足开放条件）。
  这是刻意的省钱默认，不是漏配——去档案页按供应商批量勾选即可。
- 本插件 `main.py` 无 `importlib.reload` 热重载列表：新增三个顶层模块首次升级没问题，
  但以后改这三个文件的内容需完整重启 AstrBot 才生效。
- 验证：后端累计 190+ 项断言（规则引擎 / 存储迁移 / 巡检集成 / 定时探测 / 流式探测 /
  指令检测），前端用桩桥注入构建产物做了浏览器冒烟（含交互与 i18n 键泄漏扫描）。
  **未在真实 AstrBot 实例上跑过**，`provider_stats` 实读与 `send_message` 实投递待验。

## v1.2.9

- 修复「插件模型」页看不到插件里**还没填值**的模型配置项
  （典型：comfyui-anima 的 `translate_llm_model`「LLM 翻译用模型」、`llm_model`
  「理解指令并调用工具的大模型」——这两个都是 `_special: select_provider` 的官方入口，
  没配时值就是空字符串）
  - 现象：用户反馈"绘图插件里也有几个配置模型的地方，为啥没有展示出来"
  - 根因：v1.2.6 的规则是"只列非空值"（当时为了不让几十个空 key 淹没页面），
    schema `_special` 入口为空时被直接跳过，于是这些**配置入口**整条消失
  - 修复（后端）：pass1（schema `_special`）**空值也收集**，新增 `configured` 标记；
    条目按"已配置在前、未配置沉底"稳定排序；插件级新增
    `special_slots` / `special_configured` / `special_unconfigured` 统计，
    扫描日志与 stats 同步输出（便于日后一眼看出"是没配还是没识别到"）
  - 修复（前端）：新增「显示未配置项」开关（**默认开启**）；未配置项显示灰字
    「未配置」并可直接在下拉里选模型（写回逻辑无需改动，空字符串原值本来就允许写）；
    分组头部标签改为「x 已配置 · y 未配置」；另加一行提示"还有 N 个低可信项未显示"
    （点击即可展开），避免用户误以为漏了内容
  - 说明：值命中 / 键名提示两类仍然只收非空值（它们本来就有值），
    因此不会因为这次改动产生额外噪音；如果嫌伴侣插件那几十个空模型入口太长，
    关掉「显示未配置项」即可

## v1.2.8

- 隐藏指令 `/陪伴分数` 增强：新增「当前互动状态」一行
  - 背景：伴侣插件里"**长期关系阶段**"（由好感度分数决定，八阶段）与
    "**当前互动状态**"（短期互动温度，受边界、被刺到、重新接近等事件影响）是两个维度，
    原指令只显示了分数与阶段
  - 互动状态七档：回避 / 受伤 / 放松 / 活泼 / 温暖 / 亲近 / 爱意
    （取自伴侣插件 `ExpressionBand` / `EXPRESSION_BAND_LABELS`，按从冷到暖排序用于比较）
  - 数据源与兼容：用户档案的 `current_interaction`；档位键名兼容
    `expression_band` / `band` / `state` / `mode`，也兼容只写了中文 `label` 的档案。
    档案里没有该字段（旧档案 / 未开启关系距离感）时，按伴侣插件 `_baseline_band`
    规则用分数推算，并在括号里明确标注「按好感度推算」，不谎报为真实状态
  - 复刻伴侣插件投影的归一化语义（否则会显示插件运行时不会用的档位）：
    普通用户不能进入「亲近 / 爱意」（降到温暖）、不得超过
    `normal_interaction_band_cap`（默认温暖）、`expires_at` 过期后回到放松
  - 括号内标注来源与时效：自动判定 / 管理员设置 / 按好感度推算 / 原状态已过期 /
    受互动温度上限限制 / 有效期至 `MM-DD HH:MM`；`reason` 只在写了中文说明时展示
    （内部错误码不外露）
  - 顺带核对：关系阶段标签与伴侣插件 `relationship_policy._DEFAULT_STAGES` 的八阶段
    完全一致（极度疏离 / 强烈疏离 / 疏离 / 初识 / 熟悉 / 亲近 / 亲密 / 深度联结），未改动

## v1.2.7

- 修复「分组一键测试」不遵守勾选规则：分组内未勾选的模型也会被真实检测（既浪费额度，
  也违反"勾选 = 参与检测"的约定）
  - 根因：前端 `testMany(ids)` 在分组模式下把 `skip` 直接置空（`ids ? [] : …`），
    后端 `api_test_all_stream` 又只按 `ids` 缩小范围、不剔除未勾选项 ——
    于是"取消勾选"实际只对一键检测生效
  - 修复（前端 + 后端双保险）：
    - `DetectView.testMany` 无论全测还是分组测都传完整 skip；判定语义与复选框一致
      （`enabled[k] === false` 才算未勾选，undefined 视为已勾选）
    - 新增 `ModelPanelPlugin._skip_ids()`：以 storage 里的检测开关偏好兜底合并 skip，
      `api_test_all_providers` / `api_test_all_stream` 都改用，保证任何调用路径下
      「没勾选就不测」
  - 分组内被跳过的模型仍会出现在结果里（标「已跳过」），只是不再发真实请求
- 修复「插件模型」页把插件自己的业务模型当成模型配置列出
  （如 comfyui-anima 的 LoRA 文件名 `anima_lacrimosa.safetensors`、工作流底模 `illustrious`）
  - 根因：键名提示（`model_name` / `base_model` 命中 "model"）对 `template_list`
    模板内的字段同样生效；而模板条目是用户自定义的列表项，里面的模型字段属于
    该插件自己的业务（绘图模型、LoRA 文件），并不是 AstrBot 的 provider
  - 修复：新增 `_is_template_field()`，`template_list` 模板内的字段**只认**
    schema `_special` 声明与值命中，不做键名提示；该方法同时兼容具体配置路径
    （`loras.0.model_name`）与 schema 占位路径（`loras.None.model_name`）
  - 前端「低可信项」默认改为**不显示**（此前默认显示）：这类条目绝大多数就是
    插件自己的业务模型字段（如 `nai_model`），需要排查时再勾选显示
- 修复 provider 目录不含重排序模型，并补上"默认 provider"信息
  - AstrBot 支持的 provider 有 5 类：chat / tts / stt / embedding / rerank，
    少一类就会出现「插件里明明配了这类模型，却被标成未匹配、下拉里也选不到」
  - `rerank` 在 AstrBot 4.27.4 的 `Context` 上**没有 getter**，改为直接取
    `provider_manager.rerank_provider_insts`
  - 非对话 provider 的模型名可能写在 `embedding_model` / `rerank_model` /
    `tts_model` / `stt_model` 等字段，补进取值兜底
  - provider 目录新增 `is_default`：chat 取 `provider_settings.default_provider_id`
    （及 `default_chat_provider_id` 快照），tts / stt 取
    `provider_tts_settings.provider_id` / `provider_stt_settings.provider_id`，
    前端下拉里标「（默认）」。embedding / rerank 在 AstrBot 主配置里**没有**
    全局默认项（由使用它们的插件各自指定），故不标

## v1.2.6

- 新增「插件模型」页：自动扫描**所有已安装插件**的配置，把其中的模型项列出来，
  可直接单项更换，也可按「旧模型 → 新模型」批量替换
  - 需求：换模型时不该只盯着陪伴插件——别的插件（视觉模型、TTS、向量模型…）
    也各自配了一份模型，全靠人工去每个插件的配置页翻找，容易漏
  - 识别来源按可信度分三级，前端逐条标注：
    1. **schema 声明（high）**：`_conf_schema.json` 里 `_special` 为
       `select_provider` / `select_provider_tts` / `select_provider_stt` 的配置项。
       这是 AstrBot 官方约定，值一定是 provider id，识别最准
    2. **值命中（high / medium）**：配置树里任意字符串值等于某个已加载 provider 的 id
       （high）或模型名（medium）；JSON 字符串里套着 `{key: provider_id}` 映射的
       （如陪伴插件 `model_fallback_overrides`）会展开成多条
    3. **键名提示（low）**：键名含 `provider` / `model`、值为非空短字符串，
       但既不是已知 id 也不是已知模型名（典型场景：这个模型已经被删掉了）。
       标注「低可信」，可一键隐藏
  - 后端新增模块 `plugin_models.py`：
    - 拍平 `_conf_schema.json`（含 `object` 分组与 `template_list` 模板）得到「路径级 schema 表」
    - 递归遍历插件 config；跳过 `__` 前缀的 AstrBot 内部字段（如 `__template_key`）
    - **镜像去重**：同一个 key 既在顶层扁平副本又在分组嵌套里（陪伴插件就是这种结构），
      值相同时只保留最深的一条并标注「镜像」；值不同则两条都列并标「配置不一致」
    - **写回**：按 path 精确写回（支持 dict 键、list 下标、JSON 映射内层）；
      陪伴插件走 `_flat_set` 语义全量同步（否则「改了配置、插件读到的还是旧值」）；
      写前校验原值必须是字符串，路径失效则报错且不改
    - **运行时同步**：插件实例属性名 == 配置键名、且属性当前值 == 我们看到的旧值时，
      才 `setattr` 同步（很多插件 bootstrap 时把配置读进实例属性、之后不再读 config）；
      其余情况由前端提示「可能需要重载插件」
    - 误报防护：schema 带 `options` 的枚举项、非字符串类型不参与「键名提示」
      （否则 `provider_config_mode=quick` 这类会被当成模型）；
      JSON 映射内层只信「值确实命中」，避免把 `task_prompt_overrides` 里的提示词正文
      当成模型（它的内层键名同样带 provider）
  - 前端新增 `webui-src/src/views/PluginsView.vue`（导航「插件模型」）：
    - 按插件折叠展示：用途（优先取 schema description）/ 配置键 / 当前模型 / 更换为 / 状态，
      条目里可展开看 schema `hint`
    - 顶部支持搜索、显示低可信项、隐藏无模型配置的插件，并统计「N / M 个插件含模型配置」
    - **批量替换**：选一个「旧模型」→「新模型」，只作用于当前筛选出的列表，弹窗确认后再写
    - 状态标签：未匹配 / 配置不一致 / 低可信 / 镜像；TTS、STT、向量模型单独标注类型；
      更换下拉按 provider 类型分组，同名模型附加 provider id 后缀区分渠道
  - 新增接口：`GET /panel/plugin_models`（扫描，只读）、
    `POST /panel/plugin_models/set`（按 `{plugin, path, value}` 批量写回）
  - 与「陪伴插件」页的关系：陪伴页保留主模型 / 备用模型的专用语义（按模型名批量替换、
    同步陪伴插件实例属性）；本页是通用视图。两者读写同一份配置，改完互相可见

## v1.2.5

- 修复陪伴插件页模型选择在**同名模型**场景下只显示一个提供商的问题：
  不同 provider 渠道配置同一个模型名（如两个 openai 中转都用 `gpt-4o`）时，
  后端 `/panel/providers` 构建 `provider_models` 按 model 名去重，
  只保留第一个渠道，其余渠道在下拉里消失（默认模型页不受影响，
  它直接使用全量 provider 列表）
  - 后端：`provider_models` 改为每个 provider 一条、全量返回（不按 model 去重）
  - 前端：下拉对重复的 label（vendor · model 相同）附加 provider id 后缀，
    保证每个渠道都可区分；写回值仍是 provider id，与陪伴插件配置直接匹配

## v1.2.4

- 修复多语言缺 key 导致界面显示原始 key 的问题：
  - 模型检测「查看详情」按钮悬浮提示与详情弹窗的 11 个字段
    （`detect.detail.*`）在四个语言文件里写成了带点号的扁平 key，
    vue-i18n 按 `.` 拆嵌套路径查不到，回退显示 `detect.detail.id` 之类的原文；
    现已全部改为嵌套结构（zh / en / ja / ko）
  - en.json 文件末尾存在重复的顶层 `nav` 对象（仅含 settings），
    JSON 同名 key 相互覆盖，导致英文界面导航丢失 4 个翻译回退显示中文；已合并
- 修复陪伴插件页面高度异常：scoped 样式里本意控制弹窗的
  `:deep(.n-card)` 限高规则实际命中的是页面主体卡片
  （弹窗 teleport 到 body 后 scoped 根本管不到），
  导致主体卡片底边超出可视区、需滚动父元素才能看全，且内容区双层滚动；
  现已移除该规则，主体卡片恢复自然高度，弹窗尺寸仍由全局样式正确控制
- 打包脚本 `_bump_version.ps1` 版本正则从写死的 `v0.x.y` 改为通用
  `v{major}.{minor}.{patch}`，进入 1.x 后自动 bump 不再误判为"无版本号"

## v1.2.3

- metadata 信息修正：
  - `author` 改为「涟漪」（原 `local`）
  - `category` 改为 `utilities`（工具类）。AstrBot 市场分类使用英文 key 枚举
    （ai_tools / utilities / productivity / integrations / entertainment / other），
    中文标签由 Dashboard 映射；原先填写的中文「AI 增强」无法被市场分类筛选正确归并

## v1.2.1

- 回退对话模型列表视觉与交互重做（改为卡片化，解决"不够醒目、小家子气"）：
  - 每个模型独占一张卡片：主标题显示模型名、副标题显示供应商，
    序号改为醒目的圆形徽标，内边距与字号整体加大
  - 右侧操作按钮加大到 30×30，删除键与上下移之间加分隔线，缓解误触
  - 拖拽更易发现：整张卡片即可拖动（光标变抓手），手柄改为点阵抓手图标，
    右栏标题下常驻「按住卡片可上下拖拽调整顺序」提示，拖拽落位显示插入指示线
  - 左右两栏等高对齐：列表区用 flex 撑满，右栏补一行与搜索框等高的提示行，
    列表最小高度提升到 288px
- 左侧模型改为**选中态切换**：点击后保持显示并高亮（角标显示回退序号），
  同时加入右侧；再点一次取消选中并从右侧移除；移除原来的加号图标

## v1.2.0

- 回退对话模型列表改为**拖拽排序**（原来的多选下拉无法表达回退优先级）：
  - 左侧「可选模型」支持关键字搜索，点击即加入右侧
  - 右侧「回退顺序」列表可拖拽调整，序号即优先级（1 为最先尝试）
  - 拖拽时目标项显示插入指示线；拖到列表空白处则移到末尾
  - 保留上移 / 下移 / 删除按钮，方便触屏与无鼠标场景
  - 已选模型不再出现在左侧，避免重复添加
- 拖拽用原生 HTML5 拖放实现，未引入 sortablejs 等额外依赖（不增加包体积）

## v1.1.0

- 首页控制台改版，新增用量统计：
  - 顶部新增「今日 Token」统计卡片（与模型数 / 默认模型 / 陪伴插件并列）
  - 新增 Token 用量区块：今日用量、累计用量、今日请求次数，
    并拆分展示输入 / 输出 / 缓存 token
  - 新增近 7 天用量柱状趋势图（纯 CSS 实现，不引入图表库）
  - 新增今日模型用量 Top 排行（按模型聚合，附带请求次数）
- 用量采集：新增 `on_llm_response` 钩子，从 `LLMResponse.usage`
  （input_other / input_cached / output）记录每次调用，写入新表 `llm_usage`；
  流式分片与 usage 为空的调用跳过，避免重复计数
- 模型归属：按响应里的模型名反查 provider id，带缓存，避免每次调用都枚举 provider
- 用量数据保留 90 天，插件启动时自动清理过期记录
- 默认模型展示改为「供应商 · 模型」友好名，并保留 provider id 便于核对
- 卡片样式优化：圆角、悬浮抬升、强调色数字、等宽数字对齐；
  数字超过千/百万自动缩写为 k / M

## v1.0.0

- 默认模型页升级为**可配置**（此前只是只读展示），支持三项：
  1. 默认对话模型：下拉选择，留空则由 AstrBot 使用第一个可用模型
  2. 回退对话模型列表：多选并按顺序保存，主模型请求失败时依次切换
  3. 默认图片转述模型：下拉选择，留空表示不使用图片转述
- 兼容新旧两套 AstrBot 配置结构：新版写入
  `agent_runner.config.model.provider_id` / `fallback_provider_ids`，
  同时镜像旧键 `provider_settings.default_provider_id` / `fallback_chat_models`，
  旧版 AstrBot 也能正常生效；读取时同样新旧择优
- 保存后同步 provider_manager 的 `default_chat_provider_id` 运行时快照
  （该属性是加载时快照，不随配置改动刷新），改完即时生效
- 页面展示「当前实际生效」的对话模型，便于核对配置是否真的落到了运行时
- 版本进入 1.0.0

## v0.6.12

- 陪伴插件新增「总览」页：列出所有用途（key）与当前主模型 / 备用模型，一眼看清每个场景配了什么模型
- 未配置项现在可直接在总览页的主模型 / 备用模型下拉里选择并即时保存（新增 `POST /panel/companion/set` 接口，单 key 直接设 / 清 provider_id，并同步陪伴插件运行时实例属性，改完即时生效）
- 升级版本至 v0.6.12

## v0.6.11

- 修复陪伴插件「私读视觉模型」（`PRIVATE_READING_VISION_PROVIDER_ID`）在替换页显示成
  `<object object at 0x...>` 的 bug：根因是 `_flat_get` 找不到配置项时返回哨兵 `_MISSING`
  （一个 `object` 实例），而取值处用 `if raw` 误判其为真值并 `str()` 成怪异字符串；
  现已显式排除哨兵，未配置项正确归到「未配置」区

## v0.6.10

- 打开替换页时若发现陪伴插件内存里的备用模型落后于配置，自动纠正一次
- 修复陪伴插件「备用模型」替换后不生效：只 save_config() 写文件不会刷新陪伴插件
  运行时的 `model_fallback_overrides` 实例属性（其 WebUI 优先读实例属性），
  现在替换后会用陪伴插件自带的归一化方法同步该属性，改完即时可见
- 备用模型写回改为保留未收录的 provider key，避免陪伴插件升级新增 key 后
  被我们静默抹掉
- 备用模型配置不再写入 `model_assignment_config` 分组（它是 legacy 顶层配置）
- 补齐陪伴插件新增的 provider key `REACTION_EXPRESSION_EMBEDDING_PROVIDER_ID`
- 替换结果区分主模型 / 备用模型数量；陪伴插件内存值仍不一致时给出提示

## v0.6.8

- 安全审查：API 鉴权、输入校验、文件读写范围均符合规范，无已知安全问题
- 例行发布，功能与 v0.6.7 一致

## v0.6.7

- 新增更新日志 CHANGELOG.md（插件市场可查看版本记录）
- 规范 metadata.yaml：补充 `documentation` 更新日志引用与 `short_desc` 短描述
- 模型检测：分组单独一键测试、每行展示最近检测时间
- 陪伴插件模型替换：支持主模型 / 备用模型一键批量替换（精简配置 + 主次区分双 Tab）
- 检测记录持久化到 SQLite（WAL），为历史记录与统计图预留
- WebUI 萌化风格、新增图标、支持中 / 英 / 日 / 韩四语
- 各页面增加刷新按钮

## v0.6.0

- 插件更名「萌萌模型控制台」，定位自用：快速更换「我会永远陪着你」陪伴插件的模型
- 新增模型检测（延迟 + 存活）、默认模型查看、陪伴插件精准模型替换
